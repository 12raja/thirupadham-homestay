server.port=8082
spring.application.name=notification-service

# Twilio credentials - set via environment variables/Secret, never here.
twilio.account-sid=${TWILIO_ACCOUNT_SID:}
twilio.auth-token=${TWILIO_AUTH_TOKEN:}
twilio.whatsapp-from=${TWILIO_WHATSAPP_FROM:whatsapp:+14155238886}

# Owner's real WhatsApp number, e.g. +917708095432
owner.whatsapp-number=${OWNER_WHATSAPP_NUMBER:}
